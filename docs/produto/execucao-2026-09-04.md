# Execução autorizada em 04/09/2026

## Continuação após autorização de push na main

O fundador autorizou o push e a continuação. A primeira entrega está em `bb75395`,
enviada à `main`. O [CI em Linux](https://github.com/ArthurMoreiraS/causor.ai/actions/runs/33931956890)
passou no backend e no frontend, incluindo o pacote de produção que o Windows
não conseguiu montar por permissões de symlink.

O workflow de deploy dessa versão marcou sucesso indevidamente: os logs mostram
`denied` no download das imagens e o script continuou até o `/health` da versão
antiga. Esse resultado **não comprova implantação**. A continuação corrige a
causa: token temporário do job com `packages: read`, credencial Docker isolada
e removida ao terminar, parada na primeira falha, Compose do mesmo commit,
espera pelos serviços e conferência da imagem de backend, worker, autos-worker
e frontend. Configuração e tag anteriores recebem cópias para recuperação.
Uma falha depois de iniciar a atualização exige diagnóstico; não há promessa
de rollback automático de banco/serviços.

A redação agora preserva **todo o inventário e todos os resumos** e seleciona
excertos por correspondência de termos com a intimação/providência. Pelo menos
um excerto por documento já representado nas citações entra antes dos demais.
Nenhuma citação é cortada. Se o conteúdo obrigatório não couber, a geração
retorna `draft_context_budget_exceeded`, com explicação na interface. A seleção
registra hash, tamanho, fontes, contagens e omissões no dossiê; a interface
identifica quais fontes foram enviadas ao redator. A cronologia suplementar
pode ser reduzida com aviso, preservando as linhas mais recentes.

Configurações: `CAUSOR_DRAFT_HISTORY_MAX_BYTES=120000` e
`CAUSOR_DRAFT_PROMPT_MAX_BYTES=180000`. São limites em **bytes UTF-8**, não uma
contagem exata de tokens; precisam ser dimensionados para o modelo escolhido.
O teto final inclui instruções, metadados, template, teor e schema antes da
chamada de redação. Não há busca semântica nem garantia de que a seleção lexical
identifica todas as provas relevantes. Resumos ainda precisam de avaliação
jurídica; processos cujos resumos excedem o limite aguardam ajuste de capacidade.

Quando a classificação diverge de um prazo cadastrado, o redator usa a duração,
o regime de contagem e a data persistidos. O dossiê preserva a sugestão original
da IA e registra a divergência e o prazo utilizado para revisão.

Validação local desta continuação: suíte completa com **630 testes backend
aprovados e 6 pulados**, mais **5 testes do script de deploy** com Docker/rede
simulados; frontend com **70 testes**, ESLint e TypeScript; Ruff aprovado.
O deploy corrigido precisa ser confirmado pelos logs do próximo workflow.

Próxima entrega interna: concorrência, recuperação e migrações em Postgres,
seguida de proteção de auditoria no banco. Avaliação de minutas continua
dependendo de casos autorizados e revisão por advogado; integração judicial
continua dependendo do primeiro acesso real.

Pedido do fundador: transformar o diagnóstico em plano e executar. Uma pessoa desenvolve o produto; tribunal, acesso e parceiro do piloto ainda não foram definidos.

## Entregas e aceites

1. **Fluxo documental operacional:** separar consumidores da fila; ligar extração, resumo citado e contexto; executar continuamente no ambiente de produção; permitir retomada; upload e declaração fundamentada de grau não aplicável pela interface. Aceite: teste HTTP → worker → contexto pronto, sem escrita manual de contexto no banco.
2. **Estados confiáveis:** interface usa o mesmo diagnóstico do gate; upload não afirma completude externa; login não anuncia conector inexistente. Aceite: regressões de captura concluída com resumo pendente e acesso sem driver.
3. **Prazos e alertas:** retirar vencimentos presumidos; explicitar revisão pendente; simulação de email não consome deduplicação. Aceite: comunicação ambígua não recebe 15/1 dias; falha pode ser retomada.
4. **Revisão e protocolo assistido:** invalidar aprovação ao editar, distinguir declaração manual de recibo conferido e preservar evidências. Aceite: conteúdo modificado não segue aprovado; ausência de recibo não recebe selo positivo.
5. **Qualidade e modelos:** reforçar fontes, preparar comparação controlada do Astra com o provedor atual e registrar os limites. A avaliação jurídica depende de exemplos autorizados e de revisor; não será substituída por testes sintéticos.

Aplicar testes pertinentes durante cada entrega e verificações de integração ao final. Não executar protocolo judicial, migração remota ou chamadas pagas para simular homologação. Registrar aqui o resultado real e em `docs/estado.md` as mudanças operacionais.

## Dependências externas

Piloto: advogado, família de atos, casos autorizados e tribunal/sistema. Protocolo automático: acesso e homologação no destino escolhido. Escala: teste de concorrência e migrações em Postgres equivalente à produção, monitoramento e política de auditoria. Essas etapas permanecem explícitas no plano de evolução.

## Resultado

Implementação local das cinco entregas internas. Nenhum deploy, protocolo judicial ou ensaio pago de modelo foi executado. O fundador confirmou durante a execução que não tem acesso a nenhum tribunal/sistema.

| Entrega | Resultado no código | Limite |
|---|---|---|
| Autos | Worker processa PDF → trechos → resumo citado → contexto; consumidor OAB filtra seus jobs; Compose inclui `autos-worker` | Serviço configurado, ainda não implantado |
| Retomada | Botão reenfileira falhas e jobs legados concluídos sem resumo; etapas completas são reutilizadas | Jobs antigos presos em `running` exigem diagnóstico operacional; não são retomados enquanto possam estar ativos |
| Estado | Gate, endpoint de próximo passo e painel consultam prontidão/fingerprint reais | Readiness cobre o escopo recebido, não prova o conjunto disponível no tribunal |
| Graus | Upload seleciona 1º/2º grau; ausência tem declaração identificada e justificada | Declaração humana é distinguida de evidência do tribunal |
| Prazos | Captura deixou de criar 15 dias; classificação ausente/inválida vira `null`; prazos existentes são reutilizados; confirmação manual calcula e audita a origem | Números identificados pela IA continuam exigindo revisão; registros legados não foram apagados ou reclassificados sem evidência |
| Alertas | Console não consome deduplicação; falha/simulação ficam auditadas; CLI retorna código não zero nesses casos | Entrega real por SMTP não testada |
| Aprovação | PDF da revisão é persistido por hash; aprovação registra hash; edição invalida aprovação; envio real usa snapshot aprovado | Anexos e seleção de grau de protocolo ainda não formam pacote completo; não liberar automação antes de fechar essa etapa |
| Comprovante | Declaração manual registra usuário efetivo e distingue ausência/referência não verificada | URL ou número informado não conferem autenticidade do recibo |
| Fontes | Dossiê abre a versão citada do PDF por página; hash e escopo do documento são conferidos; resumo sem citação é rejeitado | Citação literal válida não prova que toda conclusão jurídica decorre dela |
| Modelo | Adapter OpenAI Responses opt-in por tarefa, schema estrito, recusa/saída incompleta rejeitadas; harness com contexto fixo | Acesso à API e qualidade jurídica do Astra não foram aferidos |

## Operar a entrega

Com banco, autenticação, storage e provedor já configurados conforme o setup do repositório:

1. Abrir o processo e enviar o conjunto de PDFs do grau escolhido. Cada envio define o inventário daquele grau; anexar só uma peça em um novo envio não equivale a completar o lote anterior.
2. Enviar o outro grau ou declarar, com justificativa, que o processo não possui autos nele.
3. Executar o consumidor documental a partir de `backend`: `python -m app.autos.worker`. Para um ciclo: `python -m app.cli process-autos-due`. O worker geral continua responsável pela captura OAB. No Compose de produção, o consumidor documental passa a ter serviço próprio.
4. Acompanhar extraídos/resumidos. Usar **Retomar processamento com falha** também para documentos legados que ficaram só na extração. Corrigir a causa do erro do provedor/storage antes de tentar novamente.
5. Na intimação, gerar a minuta e revisar fatos, fontes e providência. Se não houver prazo identificado, a peça sinaliza a pendência. A intimação oferece confirmação da duração/data base e das exceções do calendário. Essa contagem pressupõe calendário nacional/recesso cível; outros regimes e suspensões precisam ser conferidos.
6. Salvar alterações, baixar/revisar o PDF e aprovar. Nova edição exige nova aprovação. Peças aprovadas antes desta versão precisam de nova revisão/aprovação para adquirir um snapshot utilizável no caminho de envio real.
7. Quando houver acesso ao tribunal, o advogado poderá enviar a peça externamente e registrar o resultado. O Causor apresenta esse registro como declaração, com estado do comprovante explícito.

O teste integrado em `backend/tests/test_autos_upload_api.py` percorre HTTP → worker → contexto → minuta → fonte → PDF → aprovação → registro manual, usando PDF de teste e provedores simulados. O teste não chama tribunal e não escreve um contexto pronto artificialmente no banco.

## Preparar o experimento Astra

O adapter usa Responses com Structured Outputs, sem `temperature`, e envia `store=false`. Isso é configuração da requisição, não promessa de retenção zero. Referências: [guia do Astra](https://developers.openai.com/api/docs/guides/latest-model?model=gpt-6-astra), [Structured Outputs](https://developers.openai.com/api/docs/guides/structured-outputs).

Para comparar **somente a redação** na aplicação, configurar no ambiente do backend:

```dotenv
CAUSOR_LLM_DRAFT_PROVIDER=openai
CAUSOR_OPENAI_MODEL=gpt-6-astra
CAUSOR_OPENAI_REASONING_EFFORT=low
# OPENAI_API_KEY deve estar no ambiente autorizado, nunca em arquivo versionado.
```

Classificação e resumos mantêm o provedor global configurado. Há overrides independentes `CAUSOR_LLM_CLASSIFICATION_PROVIDER` e `CAUSOR_LLM_CONTEXT_PROVIDER`. Retirar o override de redação devolve a tarefa ao provedor global. O chat com ferramentas mantém sua implementação atual; não está incluído no adapter de redação. Não há fallback silencioso que envie um caso a outro fornecedor.

Para avaliação pareada com os mesmos dados, usar `app.agent.evaluate`. Cada linha de um JSONL autorizado contém `id`, `intimacao_texto`, `classificacao` (schema do classificador), `contexto_processo` e, opcionalmente, `historico`, `prazo_fatal` e `template_conteudo`.

```powershell
# Executar a partir de backend; este comando faz chamadas pagas aos provedores escolhidos.
.\.venv\Scripts\python.exe -m app.agent.evaluate --cases artifacts/evals/casos.jsonl --output artifacts/evals/comparacao.jsonl --provider claude --provider openai --limit 10
```

O comando preserva o hash da entrada, registra saída/erro, modelo, latência e metadados disponíveis do provedor, grava progresso por tentativa e recusa sobrescrever um arquivo de resultado. Os arquivos em `backend/artifacts/` estão ignorados pelo Git. A avaliação cega pelo advogado, critérios de gravidade e conjunto reservado seguem o plano de evolução. O harness não atribui nota jurídica automaticamente.

## Verificação e pendências de implantação

Resultados finais: **620 testes de backend passaram, 6 pulados; 69 testes de frontend passaram**. `ruff check backend`, ESLint, TypeScript e `git diff --check` passaram. Os avisos de JWT pertencem às chaves curtas das fixtures de teste. O build padrão Windows compilou e gerou as sete páginas com os valores públicos fictícios do CI, mas a montagem `standalone` falhou com `EPERM` ao criar symlinks. A compilação não equivale a aprovação do pacote de produção. Validar esse pacote no CI/Linux antes do deploy; a máquina desta sessão não disponibiliza Docker.

Antes de um piloto real, permanecem: configurar/validar o ambiente Supabase, ativar o novo worker, testar concorrência/transações no Postgres e executar a revisão jurídica com casos autorizados. Não houve mudança de schema nem migração de produção nesta entrega.

O worker documental mantém uma transação durante extração/LLM para não deixar um job confirmado como `running` após morte do processo. A publicação de contexto é serializada por processo. Isso é uma solução inicial; leases/checkpoints com transações curtas e recuperação comprovada em Postgres são a próxima evolução operacional.

Antes de protocolo automático: implementar/homologar um único driver com acesso real; completar o pacote de destino/anexos; revalidar aprovação na execução; tratar envio de resultado incerto sem repetir o ato; reconciliar recibos; aplicar proteção de auditoria no banco. O log comum ainda não tem garantia de imutabilidade imposta pelo Postgres.

Documentos extensos são resumidos em partes de até aproximadamente 40 mil caracteres, preservando todas as partes recebidas. A continuação descrita no início deste documento acrescenta seleção lexical de excertos e limites de entrada em bytes no redator. Busca semântica, contagem específica por tokenizer e avaliação jurídica permanecem pendentes.
