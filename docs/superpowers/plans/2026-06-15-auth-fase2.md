# Auth (Fase 2) — Login + isolamento multi-tenant — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Plugar Supabase Auth com validação de JWT no backend FastAPI e isolar toda query por `escritorio_id` do usuário autenticado, mais login no frontend.

**Architecture:** Supabase Auth emite o JWT. Uma dependency do FastAPI valida o token (HS256 com segredo do Supabase), resolve o `Usuario`/`escritorio_id` e expõe um `CurrentUser`. Um helper central aplica o filtro de tenant em todos os endpoints. `escritorio_id` é desnormalizado em `prazo`/`peticao`/`intimacao` para o filtro ficar uniforme. Frontend usa `@supabase/supabase-js` para login e anexa o Bearer token.

**Tech Stack:** Python 3.12, FastAPI, SQLAlchemy 2.0, Alembic, PyJWT (novo), pytest (SQLite in-memory). Frontend: Next.js 15, React 19, `@supabase/supabase-js` (novo).

**Spec:** `docs/superpowers/specs/2026-06-15-auth-fase2-design.md`

---

## File Structure

**Backend — criar:**
- `backend/app/auth/__init__.py` — pacote de auth.
- `backend/app/auth/jwt_auth.py` — `CurrentUser`, `decode_supabase_jwt`, dependency `get_current_user`.
- `backend/app/auth/tenant.py` — helper central de isolamento (`tenant_select`, `get_owned_or_404`).
- `backend/tests/test_auth_jwt.py` — testes da dependency.
- `backend/tests/test_tenant_isolation.py` — testes de isolamento cross-tenant ponta a ponta.
- `backend/alembic/versions/e4c2a1f09d31_add_auth_and_tenant_columns.py` — migração.

**Backend — modificar:**
- `backend/app/settings.py` — `supabase_jwt_secret`.
- `backend/app/sor/models.py` — `Usuario.supabase_user_id`; `escritorio_id` em `Prazo`/`Peticao`/`Intimacao`.
- `backend/app/api/main.py` — aplicar `get_current_user` + filtro de tenant em todos os endpoints (menos `/health`); parar de aceitar `escritorio_id`/`usuario_id` do body.
- `backend/app/api/schemas.py` — remover `usuario_id`/`escritorio_id` dos request bodies.
- `backend/app/capture/registrar.py` (e/ou `poll.py`/`normalize.py`) — carimbar `escritorio_id` na intimação.
- `backend/app/sor/seed_demo.py` — email do usuário do seed = `causorai@gmail.com`.
- `backend/pyproject.toml` — dependência `pyjwt`.
- `backend/.env.example` — `CAUSOR_SUPABASE_JWT_SECRET` + nota da conta de teste.

**Frontend — criar:**
- `frontend/lib/supabase.ts` — cliente Supabase do browser.
- `frontend/app/LoginGate.tsx` — guard de sessão + formulário de login.

**Frontend — modificar:**
- `frontend/lib/api.ts` — anexar Bearer token; remover `resolverUsuarioAtual` e `usuario_id` dos bodies.
- `frontend/app/page.tsx` — envolver a app no `LoginGate`.
- `frontend/package.json` — `@supabase/supabase-js`.

---

## Phase A — Data model & migrations

### Task 1: Adicionar colunas de auth/tenant aos models

**Files:**
- Modify: `backend/app/sor/models.py`
- Test: `backend/tests/test_auth_columns.py` (create)

- [ ] **Step 1: Write the failing test**

```python
# backend/tests/test_auth_columns.py
"""As novas colunas de auth/tenant existem e são gravaveis (SQLite-portable)."""

from datetime import date

from app.sor import models


def test_usuario_tem_supabase_user_id(db_session):
    esc = models.Escritorio(nome="E")
    db_session.add(esc)
    db_session.flush()
    u = models.Usuario(
        escritorio_id=esc.id, nome="Adv", email="a@b.com",
        supabase_user_id="11111111-1111-1111-1111-111111111111",
    )
    db_session.add(u)
    db_session.flush()
    assert u.supabase_user_id == "11111111-1111-1111-1111-111111111111"


def test_prazo_peticao_intimacao_tem_escritorio_id(db_session):
    esc = models.Escritorio(nome="E")
    db_session.add(esc)
    db_session.flush()
    proc = models.Processo(escritorio_id=esc.id, numero="1")
    db_session.add(proc)
    db_session.flush()
    intim = models.Intimacao(
        escritorio_id=esc.id, processo_id=proc.id, fonte="DJEN", fonte_id="x",
    )
    prazo = models.Prazo(
        escritorio_id=esc.id, processo_id=proc.id, data_inicio=date(2024, 1, 1),
        dias=15, dias_uteis=True, data_fatal=date(2024, 1, 22),
    )
    pet = models.Peticao(escritorio_id=esc.id, processo_id=proc.id, status="rascunho")
    db_session.add_all([intim, prazo, pet])
    db_session.flush()
    assert intim.escritorio_id == esc.id
    assert prazo.escritorio_id == esc.id
    assert pet.escritorio_id == esc.id
```

- [ ] **Step 2: Run test to verify it fails**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_auth_columns.py -v`
Expected: FAIL — `TypeError`/`AttributeError` (colunas inexistentes).

- [ ] **Step 3: Add the columns to the models**

Em `backend/app/sor/models.py`, na classe `Usuario` (após `oab_uf`):

```python
    supabase_user_id: Mapped[str | None] = mapped_column(String(36), unique=True)
```

Na classe `Intimacao` (após `processo_id`):

```python
    escritorio_id: Mapped[int | None] = mapped_column(ForeignKey("escritorio.id"), index=True)
```

Na classe `Prazo` (após `intimacao_id`):

```python
    escritorio_id: Mapped[int | None] = mapped_column(ForeignKey("escritorio.id"), index=True)
```

Na classe `Peticao` (após `prazo_id`):

```python
    escritorio_id: Mapped[int | None] = mapped_column(ForeignKey("escritorio.id"), index=True)
```

> Tipos portáveis (String/Integer) para o SQLite dos testes continuar funcionando. `nullable` (sem `nullable=False`) porque o backfill da migração e o capture preenchem depois.

- [ ] **Step 4: Run test to verify it passes**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_auth_columns.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/sor/models.py backend/tests/test_auth_columns.py
git commit -m "feat(sor): colunas de auth/tenant (supabase_user_id, escritorio_id)"
```

---

### Task 2: Migração Alembic (colunas novas + backfill)

**Files:**
- Create: `backend/alembic/versions/e4c2a1f09d31_add_auth_and_tenant_columns.py`

- [ ] **Step 1: Write the migration**

```python
# backend/alembic/versions/e4c2a1f09d31_add_auth_and_tenant_columns.py
"""add auth and tenant columns

Revision ID: e4c2a1f09d31
Revises: d3b1c0f5a9e2
Create Date: 2026-06-15
"""
from collections.abc import Sequence
from typing import Union

import sqlalchemy as sa
from alembic import op

revision: str = "e4c2a1f09d31"
down_revision: Union[str, Sequence[str], None] = "d3b1c0f5a9e2"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column("usuario", sa.Column("supabase_user_id", sa.String(length=36), nullable=True))
    op.create_unique_constraint("uq_usuario_supabase_user_id", "usuario", ["supabase_user_id"])

    for tabela in ("intimacao", "prazo", "peticao"):
        op.add_column(tabela, sa.Column("escritorio_id", sa.Integer(), nullable=True))
        op.create_foreign_key(
            f"fk_{tabela}_escritorio", tabela, "escritorio", ["escritorio_id"], ["id"]
        )
        op.create_index(f"ix_{tabela}_escritorio_id", tabela, ["escritorio_id"])

    # Backfill via join no processo (Postgres).
    op.execute(
        "UPDATE prazo SET escritorio_id = p.escritorio_id "
        "FROM processo p WHERE prazo.processo_id = p.id"
    )
    op.execute(
        "UPDATE peticao SET escritorio_id = p.escritorio_id "
        "FROM processo p WHERE peticao.processo_id = p.id"
    )
    op.execute(
        "UPDATE intimacao SET escritorio_id = p.escritorio_id "
        "FROM processo p WHERE intimacao.processo_id = p.id"
    )


def downgrade() -> None:
    for tabela in ("intimacao", "prazo", "peticao"):
        op.drop_index(f"ix_{tabela}_escritorio_id", table_name=tabela)
        op.drop_constraint(f"fk_{tabela}_escritorio", tabela, type_="foreignkey")
        op.drop_column(tabela, "escritorio_id")
    op.drop_constraint("uq_usuario_supabase_user_id", "usuario", type_="unique")
    op.drop_column("usuario", "supabase_user_id")
```

- [ ] **Step 2: Verify the migration applies against local Postgres**

Run (com Postgres local de pé, ver CLAUDE.md):
```bash
CAUSOR_DATABASE_URL=postgresql+psycopg://causor:causor@localhost:5432/causor ./.venv/Scripts/alembic.exe upgrade head
```
Expected: aplica `e4c2a1f09d31` sem erro. Conferir colunas com `\d prazo`.

- [ ] **Step 3: Commit**

```bash
git add backend/alembic/versions/e4c2a1f09d31_add_auth_and_tenant_columns.py
git commit -m "feat(alembic): migra colunas de auth/tenant + backfill escritorio_id"
```

---

## Phase B — Backend auth dependency

### Task 3: Dependência PyJWT + setting do segredo

**Files:**
- Modify: `backend/pyproject.toml`
- Modify: `backend/app/settings.py`

- [ ] **Step 1: Add PyJWT to dependencies**

Em `backend/pyproject.toml`, na lista `dependencies`, adicionar:

```toml
    "pyjwt>=2.8",
```

- [ ] **Step 2: Install it**

Run: `./.venv/Scripts/python.exe -m pip install -e ".[dev]"`
Expected: instala `PyJWT`.

- [ ] **Step 3: Add the setting**

Em `backend/app/settings.py`, dentro de `Settings`, após o bloco `# HTTP / CORS`:

```python
    # Auth (Supabase). Segredo HS256 do projeto; nunca logar.
    supabase_jwt_secret: str = ""
```

- [ ] **Step 4: Commit**

```bash
git add backend/pyproject.toml backend/app/settings.py
git commit -m "chore(auth): adiciona PyJWT e setting supabase_jwt_secret"
```

---

### Task 4: `CurrentUser` + dependency `get_current_user`

**Files:**
- Create: `backend/app/auth/__init__.py`
- Create: `backend/app/auth/jwt_auth.py`
- Test: `backend/tests/test_auth_jwt.py`

- [ ] **Step 1: Write the failing test**

```python
# backend/tests/test_auth_jwt.py
"""TDD da dependency de autenticação por JWT do Supabase."""

import time

import jwt
import pytest
from fastapi import HTTPException

from app.auth.jwt_auth import CurrentUser, get_current_user
from app.sor import models

SECRET = "test-secret"


def _token(sub: str, email: str, exp_delta: int = 3600) -> str:
    payload = {
        "sub": sub,
        "email": email,
        "aud": "authenticated",
        "exp": int(time.time()) + exp_delta,
    }
    return jwt.encode(payload, SECRET, algorithm="HS256")


@pytest.fixture
def _secret(monkeypatch):
    from app.auth import jwt_auth
    monkeypatch.setattr(jwt_auth.settings, "supabase_jwt_secret", SECRET)


def _make_user(db_session, sub=None, email="a@b.com"):
    esc = models.Escritorio(nome="E")
    db_session.add(esc)
    db_session.flush()
    u = models.Usuario(escritorio_id=esc.id, nome="Adv", email=email, supabase_user_id=sub)
    db_session.add(u)
    db_session.flush()
    return esc, u


def test_token_valido_resolve_usuario(db_session, _secret):
    esc, u = _make_user(db_session, sub="sub-1", email="a@b.com")
    cur = get_current_user(authorization=f"Bearer {_token('sub-1', 'a@b.com')}", session=db_session)
    assert isinstance(cur, CurrentUser)
    assert cur.usuario_id == u.id
    assert cur.escritorio_id == esc.id


def test_claim_on_first_login_grava_sub(db_session, _secret):
    esc, u = _make_user(db_session, sub=None, email="a@b.com")
    cur = get_current_user(authorization=f"Bearer {_token('sub-novo', 'a@b.com')}", session=db_session)
    assert cur.usuario_id == u.id
    db_session.refresh(u)
    assert u.supabase_user_id == "sub-novo"


def test_sem_header_401(db_session, _secret):
    with pytest.raises(HTTPException) as exc:
        get_current_user(authorization=None, session=db_session)
    assert exc.value.status_code == 401


def test_token_expirado_401(db_session, _secret):
    _make_user(db_session, sub="sub-1", email="a@b.com")
    with pytest.raises(HTTPException) as exc:
        get_current_user(
            authorization=f"Bearer {_token('sub-1', 'a@b.com', exp_delta=-10)}",
            session=db_session,
        )
    assert exc.value.status_code == 401


def test_token_valido_sem_usuario_403(db_session, _secret):
    with pytest.raises(HTTPException) as exc:
        get_current_user(
            authorization=f"Bearer {_token('sub-x', 'naoexiste@b.com')}",
            session=db_session,
        )
    assert exc.value.status_code == 403
```

- [ ] **Step 2: Run test to verify it fails**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_auth_jwt.py -v`
Expected: FAIL — `ModuleNotFoundError: app.auth.jwt_auth`.

- [ ] **Step 3: Implement the dependency**

```python
# backend/app/auth/__init__.py
```

```python
# backend/app/auth/jwt_auth.py
"""Autenticação por JWT do Supabase.

Valida o token (HS256 com o segredo do projeto), resolve o Usuario do SOR e
expõe um CurrentUser. O segredo vem de settings/env e nunca é logado.
"""

from __future__ import annotations

from dataclasses import dataclass

import jwt
from fastapi import Depends, Header, HTTPException
from sqlalchemy import select
from sqlalchemy.orm import Session

from app.settings import settings
from app.sor import models
from app.sor.db import get_session


@dataclass(frozen=True)
class CurrentUser:
    usuario_id: int
    escritorio_id: int
    email: str


def decode_supabase_jwt(token: str) -> dict:
    if not settings.supabase_jwt_secret:
        raise HTTPException(status_code=500, detail="auth não configurado")
    try:
        return jwt.decode(
            token,
            settings.supabase_jwt_secret,
            algorithms=["HS256"],
            audience="authenticated",
        )
    except jwt.PyJWTError as exc:
        raise HTTPException(status_code=401, detail="token inválido") from exc


def get_current_user(
    authorization: str | None = Header(default=None),
    session: Session = Depends(get_session),
) -> CurrentUser:
    if not authorization or not authorization.lower().startswith("bearer "):
        raise HTTPException(status_code=401, detail="autenticação requerida")
    token = authorization.split(" ", 1)[1].strip()
    claims = decode_supabase_jwt(token)
    sub = claims.get("sub")
    email = claims.get("email")

    usuario = session.scalars(
        select(models.Usuario).where(models.Usuario.supabase_user_id == sub)
    ).first()
    if usuario is None and email:
        usuario = session.scalars(
            select(models.Usuario).where(models.Usuario.email == email)
        ).first()
        if usuario is not None:
            usuario.supabase_user_id = sub  # claim on first login
            session.flush()
    if usuario is None:
        raise HTTPException(status_code=403, detail="usuário sem acesso")

    return CurrentUser(
        usuario_id=usuario.id,
        escritorio_id=usuario.escritorio_id,
        email=usuario.email,
    )
```

- [ ] **Step 4: Run test to verify it passes**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_auth_jwt.py -v`
Expected: PASS (5 testes).

- [ ] **Step 5: Commit**

```bash
git add backend/app/auth/__init__.py backend/app/auth/jwt_auth.py backend/tests/test_auth_jwt.py
git commit -m "feat(auth): dependency get_current_user com JWT do Supabase"
```

---

## Phase C — Tenant isolation helper + aplicar nos endpoints

### Task 5: Helper central de isolamento por tenant

**Files:**
- Create: `backend/app/auth/tenant.py`
- Test: `backend/tests/test_tenant_helper.py`

- [ ] **Step 1: Write the failing test**

```python
# backend/tests/test_tenant_helper.py
"""TDD do helper central de isolamento por tenant."""

import pytest
from fastapi import HTTPException

from app.auth.jwt_auth import CurrentUser
from app.auth.tenant import get_owned_or_404, tenant_select
from app.sor import models


def _two_tenants(db_session):
    a = models.Escritorio(nome="A")
    b = models.Escritorio(nome="B")
    db_session.add_all([a, b])
    db_session.flush()
    pa = models.Processo(escritorio_id=a.id, numero="A1")
    pb = models.Processo(escritorio_id=b.id, numero="B1")
    db_session.add_all([pa, pb])
    db_session.flush()
    return a, b, pa, pb


def test_tenant_select_filtra_por_escritorio(db_session):
    a, b, pa, pb = _two_tenants(db_session)
    cur = CurrentUser(usuario_id=1, escritorio_id=a.id, email="a@b.com")
    rows = db_session.scalars(tenant_select(models.Processo, cur)).all()
    assert [p.numero for p in rows] == ["A1"]


def test_get_owned_retorna_recurso_do_tenant(db_session):
    a, b, pa, pb = _two_tenants(db_session)
    cur = CurrentUser(usuario_id=1, escritorio_id=a.id, email="a@b.com")
    got = get_owned_or_404(db_session, models.Processo, pa.id, cur)
    assert got.id == pa.id


def test_get_owned_de_outro_tenant_404(db_session):
    a, b, pa, pb = _two_tenants(db_session)
    cur = CurrentUser(usuario_id=1, escritorio_id=a.id, email="a@b.com")
    with pytest.raises(HTTPException) as exc:
        get_owned_or_404(db_session, models.Processo, pb.id, cur)
    assert exc.value.status_code == 404
```

- [ ] **Step 2: Run test to verify it fails**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_tenant_helper.py -v`
Expected: FAIL — `ModuleNotFoundError: app.auth.tenant`.

- [ ] **Step 3: Implement the helper**

```python
# backend/app/auth/tenant.py
"""Isolamento multi-tenant em nível de aplicação.

Todo acesso a dados passa por aqui para filtrar pelo escritorio_id do usuário
autenticado. Recursos de outro tenant retornam 404 (não revelam existência).
"""

from __future__ import annotations

from typing import TypeVar

from fastapi import HTTPException
from sqlalchemy import Select, select
from sqlalchemy.orm import Session

from app.auth.jwt_auth import CurrentUser

T = TypeVar("T")


def tenant_select(model: type[T], current: CurrentUser) -> Select:
    """SELECT já filtrado pelo escritorio_id do usuário. `model` precisa ter
    a coluna `escritorio_id`."""
    return select(model).where(model.escritorio_id == current.escritorio_id)


def get_owned_or_404(session: Session, model: type[T], obj_id: int, current: CurrentUser) -> T:
    """Busca por id exigindo que o recurso pertença ao tenant. 404 caso contrário."""
    obj = session.get(model, obj_id)
    if obj is None or getattr(obj, "escritorio_id", None) != current.escritorio_id:
        raise HTTPException(status_code=404, detail="não encontrado")
    return obj
```

- [ ] **Step 4: Run test to verify it passes**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_tenant_helper.py -v`
Expected: PASS (3 testes).

- [ ] **Step 5: Commit**

```bash
git add backend/app/auth/tenant.py backend/tests/test_tenant_helper.py
git commit -m "feat(auth): helper central de isolamento por tenant"
```

---

### Task 6: Aplicar auth + filtro de tenant em todos os endpoints

> Mudança transversal e repetitiva. O padrão é idêntico; aplique-o a cada endpoint listado. Primeiro escreva o teste de isolamento ponta a ponta, depois faça a fiação.

**Files:**
- Modify: `backend/app/api/main.py`
- Modify: `backend/app/api/schemas.py`
- Test: `backend/tests/test_tenant_isolation.py` (create)

- [ ] **Step 1: Write the failing end-to-end isolation test**

```python
# backend/tests/test_tenant_isolation.py
"""Isolamento ponta a ponta: usuário do tenant A não vê dados do B."""

import time

import jwt
import pytest
from fastapi.testclient import TestClient

from app.api.main import create_app
from app.auth import jwt_auth
from app.sor.db import get_session
from app.sor import models

SECRET = "test-secret"


@pytest.fixture
def client(db_session, monkeypatch):
    monkeypatch.setattr(jwt_auth.settings, "supabase_jwt_secret", SECRET)
    app = create_app()
    app.dependency_overrides[get_session] = lambda: db_session
    return TestClient(app)


@pytest.fixture
def two_tenants(db_session):
    a = models.Escritorio(nome="A")
    b = models.Escritorio(nome="B")
    db_session.add_all([a, b])
    db_session.flush()
    ua = models.Usuario(escritorio_id=a.id, nome="UA", email="ua@x.com", supabase_user_id="sa")
    ub = models.Usuario(escritorio_id=b.id, nome="UB", email="ub@x.com", supabase_user_id="sb")
    db_session.add_all([ua, ub])
    db_session.flush()
    pa = models.Processo(escritorio_id=a.id, numero="A1")
    pb = models.Processo(escritorio_id=b.id, numero="B1")
    db_session.add_all([pa, pb])
    db_session.flush()
    return {"a": a, "b": b, "ua": ua, "ub": ub, "pa": pa, "pb": pb}


def _auth(sub):
    token = jwt.encode(
        {"sub": sub, "email": f"{sub}@x.com", "aud": "authenticated", "exp": int(time.time()) + 3600},
        SECRET, algorithm="HS256",
    )
    return {"Authorization": f"Bearer {token}"}


def test_health_publico(client):
    assert client.get("/health").status_code == 200


def test_sem_token_401(client, two_tenants):
    assert client.get("/processos").status_code == 401


def test_lista_so_do_proprio_tenant(client, two_tenants):
    resp = client.get("/processos", headers=_auth("sa"))
    assert resp.status_code == 200
    numeros = [p["numero"] for p in resp.json()]
    assert numeros == ["A1"]


def test_acesso_direto_a_recurso_de_outro_tenant_404(client, two_tenants):
    pb_id = two_tenants["pb"].id
    resp = client.post(f"/intimacoes/{pb_id}/draft", json={}, headers=_auth("sa"))
    assert resp.status_code in (404, 409)  # 404 por tenant; nunca 200
```

- [ ] **Step 2: Run test to verify it fails**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_tenant_isolation.py -v`
Expected: FAIL — `/processos` responde 200 sem token (auth ainda não aplicada).

- [ ] **Step 3: Wire auth + tenant filter — padrão**

No topo de `backend/app/api/main.py`, importe:

```python
from app.auth.jwt_auth import CurrentUser, get_current_user
from app.auth.tenant import get_owned_or_404, tenant_select
```

Aplique o padrão conforme o tipo de endpoint. **Exemplos completos (use como molde):**

Endpoint de **lista** — antes:
```python
    @app.get("/processos", response_model=list[ProcessoOut])
    def listar_processos(session: Session = Depends(get_session)) -> list[models.Processo]:
        return list(session.scalars(select(models.Processo)))
```
depois:
```python
    @app.get("/processos", response_model=list[ProcessoOut])
    def listar_processos(
        session: Session = Depends(get_session),
        current: CurrentUser = Depends(get_current_user),
    ) -> list[models.Processo]:
        return list(session.scalars(tenant_select(models.Processo, current)))
```

Endpoint **get/action por id** — adicione `current` e troque `session.get(Model, id)` + checagem 404 por `get_owned_or_404(session, Model, id, current)`. Ex. em `draft_peticao`:
```python
    @app.post("/intimacoes/{intimacao_id}/draft", response_model=DraftResponse)
    def draft(
        intimacao_id: int,
        payload: DraftRequest | None = None,
        session: Session = Depends(get_session),
        current: CurrentUser = Depends(get_current_user),
    ) -> DraftResponse:
        intimacao = get_owned_or_404(session, models.Intimacao, intimacao_id, current)
        ...
```

Endpoint de **escrita que recebia `usuario_id`/`escritorio_id` no body** — passe a usar `current` e remova esses campos do payload. Ex. `aprovar`:
```python
    @app.post("/peticoes/{peticao_id}/approve", response_model=PeticaoOut)
    def aprovar(
        peticao_id: int,
        session: Session = Depends(get_session),
        current: CurrentUser = Depends(get_current_user),
    ) -> models.Peticao:
        peticao = get_owned_or_404(session, models.Peticao, peticao_id, current)
        peticao.status = "aprovada"
        peticao.aprovada_por = current.usuario_id
        ...
```

Endpoint de **escrita que cria recurso** — carimbe `escritorio_id=current.escritorio_id` ao criar (ex. `POST /capturas/oab`, criação de intimação/prazo/petição quando aplicável).

- [ ] **Step 4: Apply to every endpoint (checklist)**

Aplique o padrão acima em cada um (todos passam a depender de `get_current_user`, exceto `/health`):

- [ ] `GET /dashboard/operational` — filtrar agregações por `current.escritorio_id`.
- [ ] `GET /audit` — filtrar `AuditLog.escritorio_id == current.escritorio_id`.
- [ ] `POST /jobs/capture/oab`, `GET /jobs`, `GET /jobs/{id}` — escopo por tenant (jobs do escritório).
- [ ] `GET /capturas/oab`, `POST /capturas/oab` — `tenant_select` / carimbar `escritorio_id`.
- [ ] `GET /usuarios` — `tenant_select(models.Usuario, current)`; remover `escritorio_id` query param.
- [ ] `POST/GET/PATCH /usuarios/{id}/credenciais-assinatura` e `/credenciais-assinatura/{id}/desativar` — exigir que o usuário/credencial seja do tenant.
- [ ] `POST /capture/oab` — usar `current.escritorio_id` (remover do body).
- [ ] `GET /intimacoes` — `tenant_select(models.Intimacao, current)`.
- [ ] `GET /review/queue` — filtrar por tenant.
- [ ] `GET /processos`, `GET /prazos`, `GET /peticoes`, `GET /alertas` — `tenant_select`.
- [ ] `PATCH /prazos/{id}`, `POST /prazos/{id}/cumprir` — `get_owned_or_404` + `current.usuario_id`; remover `usuario_id` do body.
- [ ] `POST /intimacoes/{id}/draft` — `get_owned_or_404`.
- [ ] `PATCH /peticoes/{id}`, `POST /peticoes/{id}/approve`, `POST /peticoes/{id}/protocolar`, `POST /peticoes/{id}/protocolar/async` — `get_owned_or_404` + `current.usuario_id`; remover `usuario_id` do body.
- [ ] `POST /chat` — exigir auth; se `processo_id` vier, validar via `get_owned_or_404`.
- [ ] `GET/POST /escritorios/{id}/templates-peticao`, `PATCH /templates-peticao/{id}` — ignorar `{id}` da URL e usar `current.escritorio_id`; `get_owned_or_404` no template.

- [ ] **Step 5: Update schemas (remover campos de identidade do body)**

Em `backend/app/api/schemas.py`, remova `usuario_id` e `escritorio_id` dos request models que os tinham (ex.: aprovação, edição de petição, cumprir/revisar prazo, captura). O backend agora deriva tudo do token.

- [ ] **Step 6: Fix existing API tests to send auth**

Em `backend/tests/test_api.py`, atualize a fixture `client`/`seeded` para: setar `monkeypatch` do segredo, criar um `Usuario` com `supabase_user_id` no escritório semeado, e enviar o header `Authorization` (espelhe o padrão `_auth` de `test_tenant_isolation.py`). Ajuste cada chamada para incluir `headers=_auth(...)` e remova `usuario_id`/`escritorio_id` dos corpos.

- [ ] **Step 7: Run the full suite**

Run: `./.venv/Scripts/python.exe -m pytest -q`
Expected: PASS (incluindo `test_tenant_isolation.py`).
Then: `./.venv/Scripts/python.exe -m ruff check .` → limpo.

- [ ] **Step 8: Commit**

```bash
git add backend/app/api/main.py backend/app/api/schemas.py backend/tests/
git commit -m "feat(api): exige auth e isola por tenant em todos os endpoints"
```

---

## Phase D — Capture carimba escritorio_id

### Task 7: Capture grava `escritorio_id` na intimação

**Files:**
- Modify: `backend/app/capture/registrar.py` (ou `poll.py` onde a intimação é criada)
- Test: `backend/tests/test_poll_oab.py` (estender)

- [ ] **Step 1: Write the failing test**

Em `backend/tests/test_poll_oab.py`, adicione um teste que após `poll_oab(..., escritorio_id=esc.id)` a intimação persistida tem `escritorio_id == esc.id` (inclusive quando não casa com processo). Espelhe os fixtures/mocks já usados no arquivo.

```python
def test_intimacao_recebe_escritorio_id(db_session):
    # ... arrange djen/datajud mocks como nos testes existentes deste arquivo ...
    result = poll_oab(db_session, oab="123", uf="SP", escritorio_id=7, djen=djen, datajud=datajud, calendar=calendar)
    intim = db_session.scalars(select(models.Intimacao)).first()
    assert intim.escritorio_id == 7
```

- [ ] **Step 2: Run test to verify it fails**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_poll_oab.py::test_intimacao_recebe_escritorio_id -v`
Expected: FAIL — `escritorio_id` é `None`.

- [ ] **Step 3: Stamp escritorio_id when creating the Intimacao**

No ponto onde `models.Intimacao(...)` é instanciada no fluxo de captura (`registrar.py`/`poll.py`/`normalize.py`), inclua `escritorio_id=escritorio_id` (o `escritorio_id` já chega em `poll_oab`). Propague o parâmetro até o ponto de criação se necessário.

- [ ] **Step 4: Run tests**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_poll_oab.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/capture/ backend/tests/test_poll_oab.py
git commit -m "feat(capture): carimba escritorio_id na intimação capturada"
```

---

## Phase E — Conta de teste no seed

### Task 8: Seed usa email da conta de teste

**Files:**
- Modify: `backend/app/sor/seed_demo.py`
- Test: `backend/tests/test_seed_demo.py` (estender)

- [ ] **Step 1: Write the failing test**

Em `backend/tests/test_seed_demo.py`, adicione:

```python
def test_seed_usa_email_da_conta_de_teste(db_session):
    seed_demo(db_session)  # use a assinatura real da função neste arquivo
    u = db_session.scalars(select(models.Usuario)).first()
    assert u.email == "causorai@gmail.com"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_seed_demo.py::test_seed_usa_email_da_conta_de_teste -v`
Expected: FAIL — email atual é outro.

- [ ] **Step 3: Update the seed**

Em `backend/app/sor/seed_demo.py`, troque o email do usuário principal para `causorai@gmail.com`.

- [ ] **Step 4: Run tests**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_seed_demo.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/app/sor/seed_demo.py backend/tests/test_seed_demo.py
git commit -m "chore(seed): usuário do seed usa email da conta de teste"
```

---

## Phase F — Frontend

### Task 9: Cliente Supabase + LoginGate

**Files:**
- Modify: `frontend/package.json`
- Create: `frontend/lib/supabase.ts`
- Create: `frontend/app/LoginGate.tsx`

- [ ] **Step 1: Install supabase-js**

Run (na pasta `frontend`): `pnpm add @supabase/supabase-js`
Expected: adiciona a dep ao `package.json`.

- [ ] **Step 2: Create the browser client**

```ts
// frontend/lib/supabase.ts
import { createClient } from "@supabase/supabase-js";

const url = process.env.NEXT_PUBLIC_SUPABASE_URL ?? "";
const anonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY ?? "";

export const supabase = createClient(url, anonKey, {
  auth: { persistSession: true, autoRefreshToken: true }
});
```

- [ ] **Step 3: Create the LoginGate**

```tsx
// frontend/app/LoginGate.tsx
"use client";

import { useEffect, useState } from "react";
import { supabase } from "@/lib/supabase";

export default function LoginGate({ children }: { children: React.ReactNode }) {
  const [ready, setReady] = useState(false);
  const [authed, setAuthed] = useState(false);
  const [email, setEmail] = useState("");
  const [senha, setSenha] = useState("");
  const [erro, setErro] = useState<string | null>(null);

  useEffect(() => {
    supabase.auth.getSession().then(({ data }) => {
      setAuthed(!!data.session);
      setReady(true);
    });
    const { data: sub } = supabase.auth.onAuthStateChange((_e, session) => {
      setAuthed(!!session);
    });
    return () => sub.subscription.unsubscribe();
  }, []);

  async function entrar(e: React.FormEvent) {
    e.preventDefault();
    setErro(null);
    const { error } = await supabase.auth.signInWithPassword({ email, password: senha });
    if (error) setErro(error.message);
  }

  if (!ready) return null;
  if (authed) return <>{children}</>;

  return (
    <div className="loginGate">
      <form onSubmit={entrar} className="loginCard">
        <h1>Causor</h1>
        <input type="email" placeholder="email" value={email}
          onChange={(e) => setEmail(e.target.value)} required />
        <input type="password" placeholder="senha" value={senha}
          onChange={(e) => setSenha(e.target.value)} required />
        {erro && <p className="loginErro">{erro}</p>}
        <button type="submit">Entrar</button>
      </form>
    </div>
  );
}
```

- [ ] **Step 4: Commit**

```bash
git add frontend/package.json frontend/pnpm-lock.yaml frontend/lib/supabase.ts frontend/app/LoginGate.tsx
git commit -m "feat(frontend): cliente Supabase e LoginGate"
```

---

### Task 10: api.ts anexa Bearer + remove hack de usuário; page envolve no gate

**Files:**
- Modify: `frontend/lib/api.ts`
- Modify: `frontend/app/page.tsx`

- [ ] **Step 1: Attach the Bearer token in `request`**

Em `frontend/lib/api.ts`, importe o cliente e ajuste `request`:

```ts
import { supabase } from "@/lib/supabase";

async function authHeaders(): Promise<Record<string, string>> {
  const { data } = await supabase.auth.getSession();
  const token = data.session?.access_token;
  return token ? { Authorization: `Bearer ${token}` } : {};
}

async function request<T>(path: string, init?: RequestInit): Promise<T> {
  const response = await fetch(`${API_BASE}${path}`, {
    ...init,
    headers: {
      "Content-Type": "application/json",
      ...(await authHeaders()),
      ...(init?.headers ?? {})
    },
    cache: "no-store"
  });
  if (response.status === 401) {
    await supabase.auth.signOut();
  }
  if (!response.ok) {
    const detail = await response.text();
    throw new Error(detail || `Request failed: ${response.status}`);
  }
  return response.json() as Promise<T>;
}
```

- [ ] **Step 2: Remove `resolverUsuarioAtual` and `usuario_id`/`escritorio_id` from bodies**

Delete `usuarioAtualId` e `resolverUsuarioAtual` (`lib/api.ts:321-334`). Em cada função que enviava `usuario_id`/`escritorio_id` no body (`editarPeticao`, `aprovarPeticao`, `cumprirPrazo`, `revisarPrazo`, credenciais, captura, etc.), remova esses campos — o backend deriva do token. Para credenciais/templates que usavam `escritorioId`/`usuarioId` no path, troque por rotas sem id (o backend usa o tenant do token) conforme ajustado no Task 6.

- [ ] **Step 3: Wrap the app in the LoginGate**

Em `frontend/app/page.tsx`, importe e envolva o conteúdo retornado pelo componente de página com `<LoginGate>...</LoginGate>`:

```tsx
import LoginGate from "./LoginGate";
// no return do componente principal:
//   return <LoginGate>{/* conteúdo existente */}</LoginGate>;
```

- [ ] **Step 4: Build to verify**

Run (na pasta `frontend`): `pnpm build`
Expected: build de produção conclui sem erro de tipo.

- [ ] **Step 5: Commit**

```bash
git add frontend/lib/api.ts frontend/app/page.tsx
git commit -m "feat(frontend): Bearer token nas chamadas e login gate na app"
```

---

## Phase G — Config & docs

### Task 11: Atualizar .env.example e runbook

**Files:**
- Modify: `backend/.env.example`
- Modify: `docs/deploy.md`

- [ ] **Step 1: Document backend env**

Em `backend/.env.example`, adicione:

```
# Auth (Supabase) — segredo HS256 do projeto (Settings → API → JWT Secret).
CAUSOR_SUPABASE_JWT_SECRET=
```

- [ ] **Step 2: Document frontend env + conta de teste**

Em `docs/deploy.md`, documente as vars do frontend (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`) e registre a conta de teste: criar no Supabase Auth o usuário `causorai@gmail.com` / `senha123`, vinculado ao tenant do seed; **apenas para dados fake**, nunca dado real.

- [ ] **Step 3: Commit**

```bash
git add backend/.env.example docs/deploy.md
git commit -m "docs(auth): env do Supabase e conta de teste"
```

---

## Validação final (manual, ponta a ponta)

- [ ] Backend up com `CAUSOR_SUPABASE_JWT_SECRET` setado; `GET /processos` sem token → 401.
- [ ] Criar `causorai@gmail.com`/`senha123` no Supabase; seed aplicado; login no frontend entra e lista os dados do tenant de teste.
- [ ] `pytest -q` e `ruff check .` limpos.
